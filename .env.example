# Copy to .env and adjust. docker-compose reads these for the app service.
HTTP_ADDR=:8080
DATABASE_URL=postgres://ovra:ovra@localhost:5432/ovra?sslmode=disable
WORKSPACE_CONFIG=workspace.yaml
YOUGILE_API_TOKEN=
LOG_LEVEL=info
